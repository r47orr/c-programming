#include <stdio.h>

int main() {

    /* can just use (\) backslash for almost everything.
    * to print out special characters or to use line breaks, tabs, just need to use \n and \t respectively.
    * to print out ",',\, just need to use \ before them.
    */
    
    printf("As in any other programming language, use \\ before a special character to actually print it.\n");

    printf("If needed be to print out a \\, just use a \\ before it, as follows \"\\\\\". \nIf needed be to print out a \' or \", \nalso use \\ before them inside the printf function.");

    return 0;
}